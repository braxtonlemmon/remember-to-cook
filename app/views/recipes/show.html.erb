<h2><%= @recipe.title %></h2>
<h2>Ingredients</h2>
<ul>
  <% @ingredients.each do |ingredient| %>
    <li><%= ingredient.name %></li>
  <% end %>
</ul>

<h2>Steps</h2>
  <ol>
    <% @steps.each do |step| %>
      <li><%= step.step %></li>
    <% end %>
  </ol>
